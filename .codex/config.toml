notify = ["/usr/bin/env", "python3", "/home/vscode/.agent-watch-hooks/agent_watch_event_bridge.py", "--source-app", "CodexCLI"]

# Repository-specific Codex configuration used to test local hooks/notifications.
# When this repo is loaded via codex, this override should take precedence over
# any notify command configured in CODEX_HOME/config.toml.

